use rand::prelude::*;

pub(crate) struct Neuron {
    inputs: Vec<u32>,
    output: u32
}

fn main() {
    // initialize random inputs
    let mut _rng = rand::rng();

    // do stuff 

    // store in output
}