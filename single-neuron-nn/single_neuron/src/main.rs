use rand::prelude::*;
use single_neuron::Neuron;

fn main() {
    let inputs: [u32; 3] = rand::random();

    // initialize neuron
    let neuron = Neuron {
        inputs,
        
    };
    
    // call compute neuron

    // other stuff
}